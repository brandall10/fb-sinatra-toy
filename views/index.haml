%a(name="Logout"){:href => '/logout'}Logout
%h2 Comments 
%ul.comments
	- @comments.each do |comment|
		= haml :comment, locals: { comment: comment }
		%br/
	%br/ 
	%form(action='/' method='POST')
		%input(type='text' name='comment[comment]')
		%input(type='submit')


